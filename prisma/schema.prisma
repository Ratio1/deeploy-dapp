generator client {
  provider = "prisma-client"
  output   = "./generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Project {
  projectHash String   @id
  name        String
  color       String
  createdAt   DateTime
  updatedAt   DateTime @updatedAt
  jobs        Job[]

  @@map("projects")
}

model Job {
  id           Int          @id @default(autoincrement())
  projectHash  String
  name         String
  jobType      String
  status       String       @default("draft")
  runningJobId String?
  stripeSubscriptionId     String?
  stripeSubscriptionItemId String?
  stripeCheckoutSessionId  String?
  stripeCustomerId         String?
  txHash       String?
  deeployJobId String?
  deployError  String?
  details      Json
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt

  project Project @relation(fields: [projectHash], references: [projectHash], onDelete: Cascade)

  @@index([projectHash])
  @@index([stripeSubscriptionId])
  @@index([stripeCheckoutSessionId])
  @@map("jobs")
}
